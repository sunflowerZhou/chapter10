spring:
  application:
    name: config-client
  cloud:
    config:
#      uri: http://localhost:8769
      #快速失败
      fail-fast: true
      discovery:
        enabled: true
        serviceId: config-server
  profiles:
    active: dev

server:
  port: 8762
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/